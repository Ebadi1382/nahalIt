import React from "react";
import Header from "../../Components/Header/Header";

const Home = () => {
  return(
    <main>
        <header>
          <Header/>
        </header>
    </main>
  )
};

export default Home;
